# TODO — Competitor-Inspired UX

1. Add hero with before/after transformation preview. Why: instant proof of value. How: place a slider or side-by-side sample in hero and gallery.
2. Add “How it works” block near CTA on `/create` and `/shop`. Why: keep users oriented mid-funnel. How: 3–4 steps with icons and expected time.
3. Add style gallery with filters. Why: showcases variety fast. How: grid + tabs by style, pet type, theme.
4. Add pricing plans + free preview CTA. Why: anchors value and lowers risk. How: 2–3 plan cards + “no card to preview” banner.
5. Add photo tips checklist + quality score. Why: better inputs reduce failed generations. How: pre-upload checklist with auto quality checks.
6. Add breed/theme discovery section. Why: improves SEO and exploration. How: chips linking to filtered gallery/search.
7. Add social proof strip (ratings, real reviews, UGC). Why: trust lifts conversion. How: import Shopify reviews or CMS and surface on home + product pages.

# TODO — Differentiators (Project Analysis)

1. Add “My Portraits” library in account. Why: retention and re-orders. How: persist generations per user; allow re-download, re-order, regenerate.
2. Add “Portrait Pack” (20–30 variations) and curated prompt sets. Why: beats single-style competitors. How: batch generation with themed prompt bundles.
3. Add customization controls (background, outfit, palette, caption). Why: personalization beyond style. How: extra prompt params + UI toggles in StepStyle.
4. Add multi-pet / family portraits. Why: higher AOV. How: allow multiple uploads and merge prompts; add product variant.
5. Add async generation queue + email/SMS when ready. Why: avoids timeouts and drop-off. How: enqueue jobs + notify; allow resume from `/account`.
6. Add frame/size preview + room mockups. Why: improves confidence in prints. How: size selector + wall mockup images/AR.
